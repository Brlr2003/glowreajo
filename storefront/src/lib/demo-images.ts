// Placeholder image for products without uploaded images
const PLACEHOLDER = "/placeholder-product.svg"

export function getProductImage(handle: string): string {
  return PLACEHOLDER
}

// Social proof images — replace with real customer photos
export const socialImages: string[] = []
